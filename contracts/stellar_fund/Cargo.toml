[package]
name = "stellar_fund"
version = "0.1.0"
edition = "2024"

[dependencies]
soroban-sdk = "21.7.7"

[dev-dependencies]
soroban-sdk = { version = "21.7.7", features = ["testutils"] }

[lib]
crate-type = ["cdylib", "rlib"]
